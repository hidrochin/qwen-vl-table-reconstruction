# Local (Mac / CPU) — data prep, evaluation, demo rendering.
# Deliberately excludes torch/unsloth so `src/` imports cleanly without a GPU.
apted==1.0.3
lxml==5.3.0
beautifulsoup4==4.12.3
datasets==3.2.0
pillow==11.1.0
jinja2==3.1.5
opencv-python-headless==4.11.0.86
numpy==2.2.2
tqdm==4.67.1
pytest==8.3.4
